#include <algorithm>

#include "testing.h"
#include "game/ratiogame.h"
#include "game/strategyvector.h"
#include "ratiogametest.h"

using namespace MaxPlus;

void RatioGameTest::Run() {
    testInitializeRandomStrategy();
};

void RatioGameTest::SetUp() {
};

void RatioGameTest::TearDown() {
};

void RatioGameTest::testInitializeRandomStrategy(void) {
    // MG: I'm afraid it seems this test has never worked... let's skip
    // RatioGame game = RatioGame();

    // StrategyVector vec = StrategyVector();
    // vec.initializeRandomStrategy(game);

}